/** @type {import('next').NextConfig} */
/**const nextConfig = {};

export default nextConfig;**/

const nextConfig = {
    images: {
      domains: ['localhost', 'localhost:8081', 'localhost:8082', 'localhost:8083'], // 여기에서 이미지 도메인을 추가합니다.
    },
  };
  
  export default nextConfig;
